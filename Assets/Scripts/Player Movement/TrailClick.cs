using UnityEngine;

public class TrailClick : MonoBehaviour
{
    //public GameObject player;

    //private void OnMouseDown()
    //{
    //    player = GameObject.FindGameObjectWithTag("Player");

    //    player.GetComponent<PlayerMovement>().RemoveTrail();
    //    player.transform.position = new Vector3(Camera.main.ScreenToWorldPoint(Input.mousePosition).x, Camera.main.ScreenToWorldPoint(Input.mousePosition).y, 0);
    //    player.GetComponent<PlayerMovement>().CreateTrail();
    //}
}
